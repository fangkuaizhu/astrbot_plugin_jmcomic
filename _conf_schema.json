{
  "client_impl": {
    "description": "客户端类型",
    "type": "string",
    "default": "api",
    "hint": "api (移动端) 或 html (网页端)"
  },
  "max_pages": {
    "description": "PDF最大页数限制",
    "type": "int",
    "default": 300,
    "hint": "超出此页数的本子会被截断"
  },
  "jm_temp_root": {
    "description": "临时文件根目录",
    "type": "string",
    "default": "/AstrBot/data/jmcomic_temp",
    "hint": "与 NapCat 共享的路径，分离部署时需保持一致",
    "advanced": true
  },
  "whitelist_enabled": {
    "description": "启用白名单模式",
    "type": "bool",
    "default": true,
    "hint": "启用后只允许白名单中的群组使用本插件"
  },
  "group_whitelist": {
    "description": "群组白名单",
    "type": "list",
    "default": [],
    "hint": "在此列表中的群组 ID 可以使用插件，白名单为空时全部拒绝"
  },
  "group_blacklist": {
    "description": "群组黑名单",
    "type": "list",
    "default": [],
    "hint": "在此列表中的群组 ID 禁止使用插件，黑名单优先于白名单"
  }
}
